package org.example.rules

import org.example.model.Claim
dialect "mvel"

rule "ICD Code Covered by Policy Type"
	when
		$claim : Claim(icdCodeCoveredByPolicyType == null)
	then
		$claim.icdCodeUnknown = true
end